# Git 仓库说明

本目录是 **独立 Git 仓库**（与 `../higgsfield-hell-grind-know-how` 分离）。

- 用途：官方开源镜像的元数据、脚本、纪律文档  
- 大体量 `folders/`、`_media_blobs/`、成片 mp4 默认 **不提交**（见 `.gitignore`）  
- 远端：请单独配置 `origin`，勿与 know-how 仓库混用  

推送示例：

```bash
cd higgsfield-hell-grind-opensource
git remote add origin git@github.com:<org>/higgsfield-hell-grind-opensource.git
git push -u origin main
```

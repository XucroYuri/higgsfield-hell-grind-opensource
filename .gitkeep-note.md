# Git 说明

本目录为**独立 Git 仓库**（官方镜像 + 本地旁路）。

- 大体积默认不入库：见 `.gitignore`（`_media_blobs/`、`folders/`、`film/*.mp4` 等）  
- 建议入库：`README.md`、`START_HERE.md`、`STRUCTURE.md`、`scripts/`、`meta/` 中的说明与中小 JSON/报告、`zh/` 治理文档  
- 并排 `folders/**/*.zh.md` 若生成，可按需决定是否入库（当前 `folders/` 整体 ignore 时需调整 ignore 策略）  

入口：`START_HERE.md`。

# 全量汉化启动清单

> 单目录双语：在 `higgsfield-hell-grind-opensource/` 仓根执行；汉化层为 `zh/`。

## 门禁（全部勾选方可全量）

### 原文镜像（仓根）

- [x] 媒体下载完成  
- [x] rename / 命名后处理完成  
- [ ] `meta/id_path_mapping.json` 抽查通过  
- [ ] **下载校验通过**：`python3 scripts/validate_source_download.py --strict`  
- [ ] **结构校验通过**：`python3 scripts/validate_source_structure.py`  
- [ ] 记录 `git rev-parse HEAD` 与 mapping 哈希到 `zh/07-对照索引/source_ref.yaml`  

### 并排汉化 + 治理

- [ ] 术语表 v0.1 已评审  
- [ ] **落盘规范已确认**：源旁 `.zh.md`，非独立中文树（见 `00-治理/并排双语落盘规范.md`）  
- [ ] 试点 ≥3 场：每条源 prompt 旁有 `.zh.md`（Brief 并排 `.zh.md`）  
- [ ] 抽检零致命缺陷；未改任何源 `.txt`/官方 JSON 语义  
- [ ] （可选）媒体硬链工具：`python3 zh/03-工具与脚本/link_media_from_source.py` — **不能替代并排 .zh.md**

### 研究仓 know-how

- [ ] 约定：优先引本仓**仓根原文**；定稿对照 **`zh/`** 用词  

## 启动命令

```bash
cd higgsfield-hell-grind-opensource

# 原文校验
python3 scripts/validate_source_download.py --strict
python3 scripts/validate_source_structure.py

# 试点：在源 prompts/ 旁新增 .zh.md（主工作）
# 例：folders/Hell Grind/Scene 26/prompts/00002_<id>.zh.md

# 可选：媒体硬链（不能替代并排译文）
python3 zh/03-工具与脚本/link_media_from_source.py --dry-run
```

## 回滚

- **不覆盖**仓根原文镜像  
- 汉化错误：降级 `approved → draft`，修词表，重跑批次  


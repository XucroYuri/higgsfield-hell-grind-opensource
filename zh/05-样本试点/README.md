# 样本试点

源仓就绪后，在此目录放入**少量**试点译文（不要等全量）。

## 建议试点集（最少 3 类）

1. **Brief 节选**：资产原则 + 技术底座相关段落  
2. **视频 prompt**：最长 `seedance_2_0` 之一（全文对照）  
3. **图像 prompt**：`gpt_image_2` 或 `nano_banana_*` 之一  

## 通过标准

- 双角色校对（译/校分离）  
- 零 S0/S1  
- 术语表无新增冲突  
- 对照索引可打开  

通过后写入：`05-样本试点/PILOT_PASS.md`（含日期与 source_ref）。  
